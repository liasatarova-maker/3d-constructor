(()=>{const data=window.LUXPRINT_DATA,$=s=>document.querySelector(s),catalog=$('#catalogScreen'),builder=$('#builderScreen'),grid=$('#productGrid'),categories=$('#categories'),search=$('#searchInput'),options=$('#optionsRoot'),price=$('#priceValue'),modal=$('#quoteModal');let activeCategory='Все',query='',current='calendar-house',qty=1,side='';const icon=t=>`<div class="product-icon product-icon--${t}"><i></i><b></b></div>`;function cfg(){return data.constructors[current]}function renderCategories(){categories.innerHTML=data.categories.map(c=>`<button class="category ${c===activeCategory?'active':''}" data-category="${c}">${c}</button>`).join('')}function renderProducts(){const list=data.products.filter(p=>(activeCategory==='Все'||p.category===activeCategory)&&(`${p.title} ${p.description}`.toLowerCase().includes(query)));grid.innerHTML=list.map(p=>`<article class="product-card ${p.configurable?'ready':''}" data-product="${p.id}"><div class="product-visual">${p.configurable?'<span class="ready-pill">Подробнее</span>':''}</div><div class="product-copy"><span>${p.category}</span><h2>${p.title}</h2><p>${p.description}</p><button>${p.configurable?'Открыть →':'Скоро'}</button></div></article>`).join('')}function renderOptions(){const c=cfg();side=c.sides[0];options.innerHTML=`<section class="option-group"><h3><span>1</span>Характеристики</h3><div class="spec-list">${c.specs.map(s=>`<div><small>${s.label}</small><b>${s.value}</b></div>`).join('')}</div>${c.sides.length>1?`<div class="side-choice">${c.sides.map((s,i)=>`<button class="side-option ${i===0?'active':''}" data-side="${s}">${s}</button>`).join('')}</div>`:''}</section><section class="option-group"><h3><span>2</span>Тираж</h3><div class="quantity"><button id="qtyMinus">−</button><strong id="qtyValue">${qty}</strong><span>шт.</span><button id="qtyPlus">+</button></div>${c.minQty>1?`<div class="min-qty-note">Минимальный тираж — ${c.minQty} шт.</div>`:''}</section>`;$('#qtyMinus').onclick=()=>changeQty(-(c.qtyStep||1));$('#qtyPlus').onclick=()=>changeQty(c.qtyStep||1);options.querySelectorAll('[data-side]').forEach(b=>b.onclick=()=>{side=b.dataset.side;options.querySelectorAll('[data-side]').forEach(x=>x.classList.toggle('active',x===b));updatePrice()})}function unitPrice(){return cfg().pricing[side]}function totalPrice(){const c=cfg(),p=unitPrice();if(p==null)return null;if(c.minimumTotals&&qty===c.minQty&&c.minimumTotals[side]!=null)return c.minimumTotals[side];return Math.round(p*qty*100)/100}function updatePrice(){const p=unitPrice(),total=totalPrice();price.textContent=total==null?'Цена уточняется':`${total.toLocaleString('ru-RU',{maximumFractionDigits:2})} ₽`;$('.price-card small').textContent=p==null?'Стоимость уточняется':`${p.toLocaleString('ru-RU',{maximumFractionDigits:2})} ₽ за 1 изделие${cfg().minQty>1?` · минимум ${cfg().minQty} шт.`:''}`}function changeQty(d){const c=cfg();qty=Math.max(c.minQty||1,qty+d);$('#qtyValue').textContent=qty;updatePrice()}function openBuilder(id){current=id;const c=cfg();qty=c.defaultQty||c.minQty||1;side=c.sides[0];$('.product-heading h1').textContent=c.title;$('.product-heading p').textContent=c.description;$('#productPreviewImage').src=c.preview||'';$('#productPreviewImage').alt=c.title;$('#previewTitle').textContent=c.title;renderOptions();updatePrice();catalog.classList.add('is-hidden');builder.classList.remove('is-hidden');scrollTo(0,0)}function openQuote(){const c=cfg(),total=totalPrice();$('#quoteImage').src=c.preview||'';$('#quoteModal h2').textContent=c.title;$('#quoteDetails').innerHTML=`${c.specs.map(s=>`<div><span>${s.label}</span><b>${s.value}</b></div>`).join('')}<div><span>Вариант</span><b>${side}</b></div><div><span>Тираж</span><b>${qty} шт.</b></div><div class="quote-total"><span>Стоимость</span><b>${total==null?'Уточняется':total.toLocaleString('ru-RU',{maximumFractionDigits:2})+' ₽'}</b></div>`;modal.classList.remove('is-hidden')}categories.onclick=e=>{const b=e.target.closest('[data-category]');if(!b)return;activeCategory=b.dataset.category;renderCategories();renderProducts()};grid.onclick=e=>{const c=e.target.closest('[data-product]');if(c&&data.products.find(p=>p.id===c.dataset.product)?.configurable)openBuilder(c.dataset.product)};search.oninput=e=>{query=e.target.value.trim().toLowerCase();renderProducts()};$('#backToCatalog').onclick=()=>{builder.classList.add('is-hidden');catalog.classList.remove('is-hidden')};$('#quoteButton').onclick=openQuote;$('#closeModal').onclick=()=>modal.classList.add('is-hidden');modal.onclick=e=>{if(e.target===modal)modal.classList.add('is-hidden')};$('#confirmQuote').onclick=()=>alert('Демонстрация готова. Следующий шаг — подключить отправку заявки в Bitrix.');renderCategories();renderProducts()})();